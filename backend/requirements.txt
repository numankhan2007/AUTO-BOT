# ── Shadow System Bot — Python Dependencies ──
# All versions pinned to exact stable releases.
# Pydantic v2 is a complete rewrite from v1 — pinning prevents breakage.

fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pydantic==2.7.1
python-dotenv==1.0.1
apscheduler==3.10.4
google-generativeai==0.7.2
